import React from "react";
import { WalletDesc } from "../components"

function WalletDescPage({ user_id }) {
    
    return (
        <WalletDesc user_id={user_id}/>
    );
}

export default WalletDescPage;